# FastAPI + GraphQL CRUD Operations 

after taking pull:

```
pip install 
```
`Dependencies:`

- pymysql (driver)
- sqlalchemy 
- fastapi 
- cryptography
- 'uvicorn[standard]' (must for websockets)








